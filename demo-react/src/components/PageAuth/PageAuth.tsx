import { Outlet } from 'react-router-dom'

const PageAuth = () => {
  // const location = useLocation()
  // const path = location.pathname
  // const [count] = useState(Math.random())
  // TODO: auth
  // if (count < 0 || path.includes('/404'))
  return <Outlet/>

  // else return <Navigate to={'/404'}/>
}

export default PageAuth
